import { Canvas, Meta, ArgTypes } from '@storybook/blocks';

import * as TextStories from './Text.stories';

<Meta of={TextStories} />

# Text Atom

<Canvas of={TextStories.Default} />

<br />

## children

Text is supplied as a child to the `Text` atom.

## as

You can choose to use the text atom as a `p` or `span` tag for flexibility. If the `as` prop is not defined, the tag will default to `p`.

<br />
<br />
<br />

## Props Table

<ArgTypes />
