import { Canvas, Meta, ArgTypes } from "@storybook/blocks";

import * as TextStories from "./Text.stories";

<Meta of={TextStories} />

# Text

<Canvas of={TextStories.Default} />

<br />

### Children

Text is supplied as a child to the `Text` molecule.

### Tag

You can choose to use the `p` or `span` tag for flexibility. If no tag is defined, the tag will default to `p`.

## Props

<ArgTypes />
