import { Canvas, Meta, ArgTypes } from "@storybook/blocks";

import * as SectionStories from "./Section.stories";

<Meta of={SectionStories} />

# Section

The `Section` atom is a div element with flex that accepts children.

<Canvas of={SectionStories.Default} />

<br />

### Children

Content is supplied as children to the `Section` atom.

## Props

<ArgTypes />
