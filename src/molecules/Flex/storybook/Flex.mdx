import { Canvas, Meta, ArgTypes } from '@storybook/blocks';

import * as FlexStories from './Flex.stories';

<Meta of={FlexStories} />

# Flex Molecule

The `Flex` molecule is a `Container` atom with flex applied.

<Canvas of={FlexStories.Default} />

For more information on props, see [Container atom](?path=/docs/atoms-container--docs).

<br />
<br />
<br />

### Props Table

<ArgTypes />
