import { Canvas, Meta, ArgTypes } from "@storybook/blocks";

import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Button

<Canvas of={ButtonStories.Default} />

<br />

### Children

Text is supplied as a child to the `Text` atom.

### Tag

You can choose to use the `p` or `span` tag for flexibility. If no tag is defined, the tag will default to `p`.

## Props

<ArgTypes />
